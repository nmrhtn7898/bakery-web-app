server:
  port: 9600

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: root
    password: 1234
    driver-class-name: org.h2.Driver
    hikari:
      jdbc-url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: create
      use-new-id-generator-mappings: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        default_batch_fetch_size: 1000
    open-in-view: false

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

test:
  users:
    noAuthorities:
      username: userId
      password: userPassword
      authorities:
    master:
      username: adminId
      password: adminPassword
      authorities:
        - user
        - admin
  clients:
    noSecret:
      clientId: noSecretId
      clientSecret:
      redirectUris: http://auth.bread.com
      grantTypes: authorization_code,implicit,password,refresh_token,client_credentials
      scopes: read,write
      authorities: user,admin
      resourceIds: auth
      codeChallenge: YmRmMTkyODk4YjJhYmM4MWQyOGNlZWYxMWJmODExMTYyMWZjY2ZhMGNjMGJjZTZlMjAwMGZlMzdmODc0MjcwZQ==
      codeVerifier: 4cc9b165-1230-4607-873b-3a78afcf60c5
      codeChallengeMethod: S256
    noScopes:
      clientId: noScopesId
      clientSecret: noScopesSecret
      redirectUris: http://auth.bread.com
      grantTypes: authorization_code,implicit,password,refresh_token,client_credentials
      scopes: noScopes
      authorities: user,admin
      resourceIds: auth
    noGrantTypes:
      clientId: noGrantTypesId
      clientSecret: noGrantTypesSecret
      redirectUris: http://auth.bread.com
      grantTypes: nograntTypes
      scopes: read,write
      authorities: user,admin
      resourceIds: auth
    noAuthorities:
      clientId: noAuthoritiesId
      clientSecret: noAuthoritiesSecret
      redirectUris: http://auth.bread.com
      grantTypes: authorization_code,implicit,password,refresh_token,client_credentials
      scopes: read,write
      authorities:
      resourceIds: auth
    noResourceIds:
      clientId: noResourceIdsId
      clientSecret: noResourceIdsSecret
      redirectUris: http://auth.bread.com
      grantTypes: authorization_code,implicit,password,refresh_token,client_credentials
      scopes: read,write
      authorities: user,admin
      resourceIds:
    master:
      clientId: adminId
      clientSecret: adminSecret
      redirectUris: http://auth.bread.com
      grantTypes: authorization_code,implicit,password,refresh_token,client_credentials
      scopes: read,write
      authorities: user,admin
      resourceIds: auth